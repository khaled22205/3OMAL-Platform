@{
    ViewData["Title"] = "تم إقفال الحساب";
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet" />

<style>
    :root {
        --primary-green: #004d00;
        --accent-yellow: #00d2ff;
        --light-bg: #f8f9fa;
        --text-dark: #2c3e50;
    }

    body {
        background-color: var(--light-bg);
    }

    .lockout-section {
        min-height: 90vh;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        background: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.3)), url("/img/hero-bg.png");
        background-size: cover;
        background-position: center;
        position: relative;
        color: white;
    }

    /* Glass Box */
    .glass-box {
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        padding: 40px;
        border-radius: 25px;
        border: 1px solid rgba(255,255,255,0.2);
        max-width: 600px;
        box-shadow: 0 8px 32px rgba(0,0,0,0.3);
        animation-duration: 1s;
    }

    .lockout-title {
        font-size: 3.5rem;
        font-weight: 900;
        text-shadow: 0 4px 10px rgba(0,0,0,0.5);
        margin-bottom: 1rem;
    }

    .lockout-text {
        font-size: 1.3rem;
        opacity: 0.95;
        margin-bottom: 2rem;
    }

    .btn-home {
        background: var(--accent-yellow);
        color: white;
        border-radius: 50px;
        padding: 12px 40px;
        border: none;
        font-size: 1.2rem;
        font-weight: bold;
        transition: all 0.3s ease;
    }

        .btn-home:hover {
            background: #00b4db;
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0,210,255,0.4);
        }

    .lock-icon {
        font-size: 4.5rem;
        margin-bottom: 1rem;
        color: var(--accent-yellow);
        text-shadow: 0 3px 10px rgba(0,0,0,0.4);
    }
</style>


<section class="lockout-section animate__animated animate__fadeIn">
    <div class="glass-box animate__animated animate__zoomIn">
        <div class="lock-icon">
            <i class="fa-solid fa-lock fa-lg"></i>
        </div>

        <h1 class="lockout-title">تم إقفال الحساب</h1>

        <p class="lockout-text">
            لقد تم إقفال حسابك مؤقتًا بسبب عدة محاولات تسجيل دخول غير ناجحة.
            <br />
            الرجاء المحاولة لاحقًا أو التواصل مع الإدارة إذا كنت تعتقد أن هناك خطأ.
        </p>

        <a asp-controller="Home" asp-action="Index" class="btn-home text-decoration-none">
            العودة للصفحة الرئيسية
        </a>
    </div>
</section>