@model Sany3y.Infrastructure.ViewModels.RegisterUserViewModel

@{
    ViewData["Title"] = "إنشاء حساب محل جديد";
}

<style>
    .register-section {
        background-color: #f8f9fa;
        min-height: 100vh;
        padding: 40px 0;
    }

    .register-card {
        border: none;
        border-radius: 20px;
        box-shadow: 0 15px 35px rgba(0,0,0,0.05);
        overflow: hidden;
        background: white;
    }

    .register-header {
        background: linear-gradient(135deg, var(--primary-green), #006400);
        padding: 40px 20px;
        text-align: center;
        position: relative;
        color: white;
    }

    .register-header::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%) rotate(45deg);
        width: 20px;
        height: 20px;
        background: white;
    }

    .form-floating > label {
        right: 0;
        left: auto;
        transform-origin: 100% 0;
        padding-right: 1.5rem;
    }

    .form-floating > .form-control {
        padding-right: 1.5rem;
        padding-left: 0.75rem;
        border-radius: 10px;
    }

    .form-floating > .form-control:focus ~ label,
    .form-floating > .form-control:not(:placeholder-shown) ~ label {
        transform: scale(.85) translateY(-.5rem) translateX(.15rem);
    }

    .step-title {
        color: var(--primary-green);
        font-weight: 700;
        margin: 25px 0 15px;
        padding-bottom: 10px;
        border-bottom: 2px solid #f0f0f0;
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 1.1rem;
    }

    .step-icon {
        width: 30px;
        height: 30px;
        background: var(--accent-yellow);
        color: var(--text-dark);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.9rem;
    }

    .custom-file-upload {
        border: 2px dashed #ddd;
        border-radius: 10px;
        padding: 30px;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s;
        background: #fafafa;
    }

        .custom-file-upload:hover {
            border-color: var(--primary-green);
            background: #f1f8e9;
        }
</style>

<div class="register-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="register-card animate__animated animate__fadeInUp">
                    <div class="register-header">
                        <h2 class="fw-bold mb-2">إنشاء حساب محل جديد</h2>
                        <p class="mb-0 opacity-75">انضم إلينا الآن وأضف محلك إلى صنايعي</p>
                    </div>

                    <div class="p-3 p-md-5">
                        <form asp-action="Register" enctype="multipart/form-data" method="post">
                            <div asp-validation-summary="All" class="alert alert-danger mb-4" role="alert" style="display:none;"></div>

                            <input type="hidden" asp-for="IsShop" value="true" />

                            <!-- بيانات مالك المحل -->
                            <div class="step-title">
                                <span class="step-icon"><i class="fas fa-user"></i></span>
                                بيانات مالك المحل
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="FirstName" class="form-control" placeholder="الاسم الأول" />
                                        <label asp-for="FirstName">الاسم الأول</label>
                                    </div>
                                    <span asp-validation-for="FirstName" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="LastName" class="form-control" placeholder="الاسم الأخير" />
                                        <label asp-for="LastName">الاسم الأخير</label>
                                    </div>
                                    <span asp-validation-for="LastName" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="NationalId" type="number" class="form-control" placeholder="الرقم القومي" />
                                        <label asp-for="NationalId">الرقم القومي للمالك</label>
                                    </div>
                                    <span asp-validation-for="NationalId" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6 d-flex gap-3 align-items-center">
                                    <label class="me-2">جنس المالك:</label>
                                    <div class="form-check">
                                        <input asp-for="IsMale" class="form-check-input" type="radio" value="true" id="genderMale" checked />
                                        <label class="form-check-label" for="genderMale"><i class="fas fa-male me-1"></i> ذكر</label>
                                    </div>
                                    <div class="form-check">
                                        <input asp-for="IsMale" class="form-check-input" type="radio" value="false" id="genderFemale" />
                                        <label class="form-check-label" for="genderFemale"><i class="fas fa-female me-1"></i> أنثى</label>
                                    </div>
                                </div>
                            </div>

                            <!-- بيانات المحل -->
                            <div class="step-title">
                                <span class="step-icon"><i class="fas fa-store"></i></span>
                                بيانات المحل
                            </div>
                            <div class="row g-3">
                                <div class="col-md-12">
                                    <div class="form-floating">
                                        <input asp-for="ShopName" class="form-control" placeholder="اسم المحل" />
                                        <label asp-for="ShopName">اسم المحل</label>
                                    </div>
                                    <span asp-validation-for="ShopName" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <select asp-for="CategoryId" class="form-select">
                                            <option value="">اختر نوع النشاط</option>
                                            @if (ViewBag.AllCategories != null)
                                            {
                                                foreach (var category in ViewBag.AllCategories)
                                                {
                                                    <option value="@category.Id">@category.Name</option>
                                                }
                                            }
                                        </select>
                                        <label asp-for="CategoryId">نوع النشاط</label>
                                    </div>
                                    <span asp-validation-for="CategoryId" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="BirthDate" type="date" class="form-control" placeholder="تاريخ الإنشاء" />
                                        <label asp-for="BirthDate">تاريخ الإنشاء</label>
                                    </div>
                                    <span asp-validation-for="BirthDate" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="Email" type="email" class="form-control" placeholder="البريد الإلكتروني" />
                                        <label asp-for="Email">البريد الإلكتروني</label>
                                    </div>
                                    <span asp-validation-for="Email" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="PhoneNumber" class="form-control" placeholder="رقم الهاتف" />
                                        <label asp-for="PhoneNumber">رقم هاتف</label>
                                    </div>
                                    <span asp-validation-for="PhoneNumber" class="text-danger small"></span>
                                </div>
                            </div>

                            <!-- بيانات التواصل للمحل -->
                            <div class="step-title">
                                <span class="step-icon"><i class="fas fa-map-marker-alt"></i></span>
                                بيانات التواصل
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <select asp-for="Governorate" class="form-select" id="Governorate">
                                            <option value="">اختر المحافظة</option>
                                            @foreach (var governorate in ViewBag.AllGovernorates)
                                            {
                                                <option value="@governorate.Id">@governorate.ArabicName</option>
                                            }
                                        </select>
                                        <label asp-for="Governorate">المحافظة</label>
                                    </div>
                                    <span asp-validation-for="Governorate" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <select asp-for="City" class="form-select" id="City">
                                            <option value="">اختر المدينة</option>
                                        </select>
                                        <label asp-for="City">المدينة</label>
                                    </div>
                                    <span asp-validation-for="City" class="text-danger small"></span>
                                </div>
                                <div class="col-md-12">
                                    <div class="form-floating">
                                        <input asp-for="Street" class="form-control" placeholder="العنوان بالتفصيل" />
                                        <label asp-for="Street">الشارع / المنطقة</label>
                                    </div>
                                    <span asp-validation-for="Street" class="text-danger small"></span>
                                </div>
                            </div>

                            <!-- بيانات الحساب -->
                            <div class="step-title">
                                <span class="step-icon"><i class="fas fa-lock"></i></span>
                                بيانات الحساب
                            </div>
                            <div class="row g-3">
                                <div class="col-md-12">
                                    <div class="form-floating">
                                        <input asp-for="UserName" class="form-control" placeholder="اسم المستخدم" />
                                        <label asp-for="UserName">اسم المستخدم (للدخول)</label>
                                    </div>
                                    <span asp-validation-for="UserName" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="Password" type="password" class="form-control" placeholder="كلمة المرور" />
                                        <label asp-for="Password">كلمة المرور</label>
                                    </div>
                                    <span asp-validation-for="Password" class="text-danger small"></span>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-floating">
                                        <input asp-for="ConfirmPassword" type="password" class="form-control" placeholder="تأكيد كلمة المرور" />
                                        <label asp-for="ConfirmPassword">تأكيد كلمة المرور</label>
                                    </div>
                                    <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                                </div>
                            </div>



                            <div class="d-grid gap-2 mt-5">
                                <button type="submit" class="btn btn-warning btn-lg fw-bold text-dark rounded-pill shadow-sm">
                                    <i class="fas fa-user-plus me-2"></i> إنشاء الحساب
                                </button>
                            </div>
                        </form>

                        <div class="text-center mt-4">
                            <p class="text-muted">
                                لديك حساب بالفعل؟
                                <a asp-controller="Account" asp-action="Login" class="text-primary fw-bold text-decoration-none">
                                    سجل الدخول الآن
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        function previewImage(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function (e) {
                    var preview = document.getElementById('image-preview');
                    preview.classList.remove('d-none');
                    preview.querySelector('img').src = e.target.result;
                }
                reader.readAsDataURL(input.files[0]);
            }
        }

        $('#Governorate').change(function () {
            var governorateId = $(this).val();
            if (!governorateId) {
                $('#City').empty().append('<option value="">اختر المدينة</option>');
                return;
            }
            $.ajax({
                url: `https://localhost:7178/api/CountryServices/GetCitiesByGovernorateId/${governorateId}`,
                type: 'GET',
                success: function (cities) {
                    var citySelect = $('#City');
                    citySelect.empty();
                    citySelect.append('<option value="">اختر المدينة</option>');
                    if (cities && cities.length > 0) {
                        $.each(cities, function (index, city) {
                            citySelect.append('<option value="' + city.id + '">' + city.arabicName + '</option>');
                        });
                    }
                }
            });
        });
    </script>
}
