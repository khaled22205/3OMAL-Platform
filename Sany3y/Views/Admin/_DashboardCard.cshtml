@model dynamic

<div class="dashboard-card card h-100">
    <div class="card-body d-flex flex-column justify-content-between">
        <h5 class="card-title">@Model.Title:</h5>
        <h1 class="card-text text-center fw-bold mb-3">@Model.Number</h1>
    </div>
</div>