@model Sany3y.Infrastructure.Models.User

<div class="modal fade" id="addUserModal" tabindex="-1" aria-labelledby="addUserModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">

            <!-- Header -->
            <div class="modal-header">
                <h5 class="modal-title" id="addUserModalLabel">إضافة مستخدم جديد</h5>
                <button type="button" class="btn-close close-left" data-bs-dismiss="modal" aria-label="إغلاق"></button>
            </div>

            <!-- Body -->
            <div class="modal-body">
                <form id="addUserForm">

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">الرقم القومي</label>
                            <input type="text" name="NationalId" class="form-control" />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">اسم المستخدم</label>
                            <input type="text" name="UserName" class="form-control" />
                        </div>

                        <!-- Name -->
                        <div class="col-md-6 mb-3">
                            <label class="form-label">الاسم الأول</label>
                            <input type="text" name="FirstName" class="form-control" />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">الاسم الأخير</label>
                            <input type="text" name="LastName" class="form-control" />
                        </div>

                        <!-- BirthDate & Gender -->
                        <div class="col-md-6 mb-3">
                            <label class="form-label">تاريخ الميلاد</label>
                            <input type="date" name="BirthDate" class="form-control" />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">النوع</label>
                            <select name="Gender" class="form-select">
                                <option value="M">ذكر</option>
                                <option value="F">أنثى</option>
                            </select>
                        </div>

                        <!-- Email & Phone -->
                        <div class="col-md-6 mb-3">
                            <label class="form-label">البريد الإلكتروني</label>
                            <input type="email" name="Email" class="form-control" />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">رقم الهاتف</label>
                            <input type="text" name="PhoneNumber" class="form-control" />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">كلمة المرور</label>
                            <input type="password" name="Password" class="form-control" />
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">تأكيد كلمة المرور</label>
                            <input type="password" name="ConfirmPassword" class="form-control" />
                        </div>

                        <!-- Roles -->
                        <div class="col-md-6 mb-3">
                            <label class="form-label">الدور</label>
                            <select id="addRoleSelect" name="Role" class="form-select">
                                @foreach (var role in ViewBag.Roles)
                                {
                                    <option value="@role">@role</option>
                                }
                            </select>
                        </div>

                        <!-- Category -->
                        <div class="col-md-6 mb-3" id="categorySection" style="display:none;">
                            <label class="form-label">التخصص</label>
                            <select id="CategoryId" name="Category" class="form-select">
                                <option value="">اختر التخصص</option>
                                @{
                                    if (ViewBag.AllCategories != null)
                                    {
                                        foreach (var category in ViewBag.AllCategories)
                                        {
                                            <option value="@category.Id">@category.Name</option>
                                        }
                                    }
                                }
                            </select>
                        </div>

                        <!-- Experience Years -->
                        <div class="col-md-6 mb-3" id="ExperienceYearsSection" style="display:none;">
                            <label class="form-label">عدد سنين الخبرة</label>
                            <input type="number" name="ExperienceYears" class="form-control" />
                        </div>

                        <!-- Price -->
                        <div class="col-md-6 mb-3" id="PriceSection" style="display:none;">
                            <label class="form-label">سعر المعاينة</label>
                            <input type="number" name="Price" class="form-control" />
                        </div>

                        <!-- Address -->
                        <div class="col-md-6 mb-3">
                            <label class="form-label">المحافظة</label>
                            <select name="Government" class="form-select" id="Governorate">
                                <option value="">اختر المحافظة</option>
                                @foreach (var governorate in ViewBag.AllGovernorates)
                                {
                                    <option value="@governorate.Id">@governorate.ArabicName</option>
                                }
                            </select>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">المدينة</label>
                            <select name="City" class="form-select" id="City">
                                <option value="">اختر المدينة</option>
                                @* Cities will be populated dynamically based on selected governorate *@
                            </select>
                        </div>

                        <div class="col-md-12 mb-3">
                            <label class="form-label">الشارع</label>
                            <input type="text" name="Street" class="form-control" />
                        </div>

                        <!-- Bio -->
                        <div class="col-md-12 mb-3">
                            <label class="form-label">نبذة</label>
                            <textarea name="Bio" class="form-control" rows="3"></textarea>
                        </div>

                    </div>

                </form>
            </div>

            <!-- Footer -->
            <div class="modal-footer">
                <button type="button" id="createUserBtn" class="btn btn-primary">إضافة المستخدم</button>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
            </div>

        </div>
    </div>
</div>

<style>
    .close-left {
        margin-left: 0 !important;
    }
</style>

<script>

    // ----- Create New User -----
    $("#createUserBtn").on("click", function () {
        let selectedRole = $("#addRoleSelect").val();
        let selectedCategory = $("#CategoryId").val();

        if (selectedRole === "Technician") {
            // والتحقق إن مفيش تخصص متختار
            if (selectedCategory === "" || selectedCategory == null) {
                toastr.error("يجب اختيار التخصص قبل تسجيل الفني!");
                return;
            }

            if ($("input[name='ExperienceYears']").val() === "" || $("input[name='Price']").val() === "") {
                toastr.error("يجب إدخال سنوات الخبرة والسعر قبل تسجيل الفني!");
                return;
            }
        }

        let formData = new FormData();

        // أرقام طويلة زي NationalId لازم تتحول String الأول
        formData.append("NationalId", $("input[name='NationalId']").val());

        // نصوص عادية
        formData.append("FirstName", $("input[name='FirstName']").val());
        formData.append("LastName", $("input[name='LastName']").val());
        formData.append("UserName", $("input[name='UserName']").val());
        formData.append("Email", $("input[name='Email']").val());
        formData.append("PhoneNumber", $("input[name='PhoneNumber']").val());

        // BirthDate لازم يبقى yyyy-MM-dd
        formData.append("BirthDate", $("input[name='BirthDate']").val());

        // Gender → IsMale (true / false)
        formData.append("IsMale", $("select[name='Gender']").val() === "M");

        // IsClient → checkbox
        formData.append("IsClient", selectedRole !== "Technician");

        // City, Street
        formData.append("Governorate", $("#Governorate option:selected").text());
        formData.append("City", $("#City option:selected").text());
        formData.append("Street", $("input[name='Street']").val());

        // Passwords
        formData.append("Password", $("input[name='Password']").val());
        formData.append("ConfirmPassword", $("input[name='ConfirmPassword']").val());

        // Role خارج ViewModel
        formData.append("role", selectedRole);
        if (selectedRole === "Technician") {
            formData.append("CategoryId", selectedCategory);
            formData.append("ExperienceYears", $("input[name='ExperienceYears']").val());
            formData.append("Price", $("input[name='Price']").val());
        }

        $.ajax({
            url: "/Admin/AddUser",
            type: "POST",
            data: formData,
            processData: false,
            contentType: false,
            success: function (res) {
                location.reload()
                toastr.success(res.message);
            },
            error: function (err) {
                toastr.error(err.message);
            }
        });
    });

    $(document).ready(function() {
        $('#addRoleSelect').on('change', function () {
            let selectedRole = $(this).val();

            if (selectedRole === "Technician") {
                $('#categorySection').show();
                $('#ExperienceYearsSection').show();
                $('#PriceSection').show();
            } else {
                $('#categorySection').hide();
                $('#ExperienceYearsSection').hide();
                $('#PriceSection').hide();
            }
        });

        $('#Governorate').change(function () {
            var governorateId = $(this).val();

            if (!governorateId) {
                $('#City').empty().append('<option value="">اختر المدينة</option>');
                return;
            }

            $.ajax({
                url: `https://localhost:7178/api/CountryServices/GetCitiesByGovernorateId/${governorateId}`,
                type: 'GET',
                success: function (cities) {
                    var citySelect = $('#City');
                    citySelect.empty();
                    citySelect.append('<option value="">اختر المدينة</option>');

                    if (cities && cities.length > 0) {
                        $.each(cities, function (index, city) {
                            citySelect.append('<option value="' + city.id + '">' + city.arabicName + '</option>');
                        });
                    }
                },
                error: function (xhr, status, error) {
                    console.error('AJAX Error:', status, error); // فقط log في console
                    // alert('حدث خطأ أثناء جلب المدن.');  <-- ممكن تشيله
                }
            });
        });
    });
</script>