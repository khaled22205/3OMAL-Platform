1. Should AI responses stream via SignalR (reusing existing infrastructure) or SSE?
2. What Gemini model name should I use? (e.g., gemini-2.0-flash, gemini-1.5-pro)
3. Do you want me to add new NuGet packages (e.g., Google.Cloud.AIPlatform.V1) or use raw HTTP to call the Gemini API?
4. For the floating AI button — should it appear on every page, or only within authenticated areas?
5. Conversation history retention — how long should inactive conversations be kept? (Current ISoftDelete pattern supports indefinite retention until user deletes)